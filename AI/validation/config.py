"""
검증 판정에 쓰는 상수를 한 곳에 모은다.

matching/config.py와 같은 원칙 — 값이 바뀔 때 고칠 곳을 한 군데로 모아둔다.
"""

# ── 이슈 유형 → 최종 등급 매핑 ──────────────────────────────────

#: BLOCK: 그 자체로 위험해서 통과 자체를 막는 것
#: REVIEW: 교사 확인이 필요한 것
#:
#: "다수아동뒤섞임"(BLOCK)은 v1에서 있었으나 폐지했다. 심각도 구분 없이
#: 다수 아동 언급은 전부 REVIEW로 통일 — 사람이 어차피 확인하므로
#: BLOCK/REVIEW를 세분화할 실익이 없었다.
ISSUE_LEVEL = {
    "진단명": "BLOCK",
    "개인정보표현": "BLOCK",
    "확정적표현": "REVIEW",
    "다수아동언급": "REVIEW",
    "추측성표현": "REVIEW",
    "감정적표현": "REVIEW",
    "위험행동표현": "REVIEW",
}

# ── 구조적으로 확인 가능한 개인정보 패턴 ──────────────────────────

#: 전화번호·주민번호 형식은 귀속 검증(LLM 판단) 없이 항상 BLOCK.
#: matching과 다른 지점 — 이건 "누구 얘기인지"와 무관하게, 이 형식의
#: 숫자가 문서에 그대로 있다는 사실 자체가 이미 위험하기 때문이다.
STRUCTURAL_PII_PATTERNS = [
    r"01[0-9]-\d{3,4}-\d{4}",   # 휴대폰 번호
    r"\d{6}-[1-4]\d{6}",         # 주민등록번호 형식
]


# ── Luna (OpenAI 호환) ──────────────────────────────────────────
# matching/config.py에서 그대로 가져옴. 두 에이전트가 같은 엔드포인트를
# 쓰지만, 폴더는 독립적으로 유지한다(트랙 B의 matching 작업과 안 얽히게).

LUNA_CHAT_PATH = "/v1/chat/completions"
LUNA_MODEL = "gpt-5.6-luna"

#: temperature는 이 모델에서 기본값(1)만 허용된다. 0을 보내면 400이 난다.
#: matching이 겪은 것과 같은 제약 — 판정의 흔들림은 프롬프트로 잡는다.
LUNA_TIMEOUT = 60.0