POSTGRES_DB=ktc
POSTGRES_USER=ktc
POSTGRES_PASSWORD=replace-with-a-long-random-password

# 브라우저가 백엔드를 부를 주소. 배포 시 실제 도메인/IP 로 바꾼다.
PUBLIC_API_BASE_URL=http://localhost:8080

# 백엔드 엔드포인트가 열리면 false 로 바꾸고 프론트 이미지를 다시 빌드한다.
USE_MOCK=true
